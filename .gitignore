# Secrets — ne JAMAIS committer (contient l'URL et le mot de passe de la base)
.streamlit/secrets.toml

# Base des signalements en local
support/tickets.db

# Fiches modifiées enregistrées à l'exécution (persistance des modifs IA)
html_fiches/_saved/

# Fichiers Excel de données (les utilisateurs déposent les leurs dans l'app ;
# inutile de publier des données HCP dans le dépôt)
*.xlsx
*.xls

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Sorties de test / affiches générées
*.png
!hcp_logo.png

# Libs PDF chargees depuis un CDN, plus besoin de les versionner
html_fiches/vendor/
